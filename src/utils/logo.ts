/**
 * Fallback tile colour behind a logo when an entry omits `logoBg`. Matches
 * Tailwind's `slate-50`. Entries set their own hex when a logo needs a
 * specific backdrop — usually a light mark that would vanish on white.
 */
export const DEFAULT_LOGO_BG = '#f8fafc';
